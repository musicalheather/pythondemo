---
apiVersion: v1
kind: Namespace
metadata:
  name: demo
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-demo
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: demo
  replicas: 5
  template:
    metadata:
      labels:
        app.kubernetes.io/name: demo
    spec:
      containers:
      - image: 359747720927.dkr.ecr.us-east-1.amazonaws.com/demoproject:67
        imagePullPolicy: Always
        name: demo
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: service-demo
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  type: NodePort
  selector:
    app.kubernetes.io/name: app-demo
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-demo
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - http:
        paths:
          - path: /*
            backend:
              serviceName: service-demo
              servicePort: 80
